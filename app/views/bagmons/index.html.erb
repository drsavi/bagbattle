<p id="notice"><%= notice %></p>
  <body>
    <% @bagmons.each do |bagmon| %>
      <div class="row">
        <div class="col s1"><%= link_to (image_tag(bagmon.image, alt: 'fun', style: 'height:400px; width:auto;')), bagmon %><%=  %></div>
      </div>
    <% end %>
  </body>
<br>

<%= link_to 'New Bagmon', new_bagmon_path %>